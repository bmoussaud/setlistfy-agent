[project]
name = "weather-mcp-server"
version = "0.1.0"
description = "MCP server exposing Weather API as tools."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.115.12",
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
    "python-dotenv"
]
